const ChefCard = ({ title, img, details }) => {
  console.log(item);

  const { title } = item;
  console.log(title);
  return (
    <div>
      <h1>{title}</h1>
    </div>
  );
};

export default ChefCard;
