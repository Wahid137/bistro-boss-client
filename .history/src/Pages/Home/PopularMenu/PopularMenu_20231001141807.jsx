import SectionTitle from "../../../components/SectionTitle/SectionTitle";

const PopularMenu = () => {
  return (
    <section>
      <SectionTitle />
    </section>
  );
};

export default PopularMenu;
