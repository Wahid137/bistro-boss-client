import SectionTitle from "../../components/SectionTitle/SectionTitle";

const Testimonials = () => {
  return (
    <div>
      <section>
        <SectionTitle heading={" "} subHeading={" "}></SectionTitle>
      </section>
    </div>
  );
};

export default Testimonials;
